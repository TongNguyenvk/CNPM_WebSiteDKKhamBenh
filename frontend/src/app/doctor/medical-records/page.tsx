// Xóa các import, interface, biến không dùng (React, MedicalRecord, error)

export default function MedicalRecordsPage() {
  // Xóa các import, interface, biến không dùng (React, MedicalRecord, error)

  return null;
}
