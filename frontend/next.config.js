/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'avatars.githubusercontent.com',
            'via.placeholder.com'
        ],
    },
}

module.exports = nextConfig 